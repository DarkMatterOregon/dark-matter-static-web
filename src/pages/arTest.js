import React from 'react'
import Layout from '../components/layout'

const ARTest = () => (
  <Layout>
    <div>Future AR test content from qr marker, to be used with Aframe</div>
  </Layout>
)

export default ARTest
